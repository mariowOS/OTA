# rollback manifest

# is
isinsiders="yes"
isprivate="no"
isbootable="yes"
isdvm="yes"

# version-properties
p="noprop"
currentversion=" " # leave blank, it'll check if isinsiders is true
previousversion=" "; # leave blank, it'll check if isinsiders is true
mirror="default"